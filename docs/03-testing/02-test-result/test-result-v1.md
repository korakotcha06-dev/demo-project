# Test Result v1 — Phase 0 (MVP)

> **สถานะ: ยังไม่มีใครทดสอบแม้แต่เคสเดียว** (สร้างโครง 2026-08-16)
> เจ้าของ: **OKOYE** · กรอกด้วย `/test-run` เท่านั้น
> เคสต้นทาง: [[../01-test-plan/test-case-v1|Test Case Specification v1]] (124 เคส) · เกณฑ์ผ่าน: [[../01-test-plan/test-plan-v1|Test Plan v1]] §7

> 🔴 **ห้ามกรอกไฟล์นี้ล่วงหน้า** — ทุกแถวต้องมาจากการลงมือรันจริงกับระบบที่ทำงานได้ · ตอนนี้ยังไม่มีโค้ดแอปพลิเคชันในโปรเจกต์เลย ([[../../../CLAUDE|CLAUDE.md]] §สถานะโปรเจกต์)

กลับไปที่ [[index|02-test-result]]

---

## 1. รอบการทดสอบ

| รอบ | วันที่ | ผู้ทดสอบ | build / commit | สภาพแวดล้อม | test data |
|---|---|---|---|---|---|
| — | *(ยังไม่มี)* | | | | |

> ⚠️ **ห้ามเริ่มรอบใหม่โดยไม่บันทึก build/commit** — ผลทดสอบที่ไม่รู้ว่าทดสอบอะไรอยู่ ไม่มีค่าเป็นหลักฐาน

---

## 2. สรุปตัวเลขรอบล่าสุด

| สถานะ | จำนวน |
|---|---|
| ✅ **PASS** | 0 |
| ❌ **FAIL** | 0 |
| 🔒 **BLOCKED** (รันไม่ได้) | 0 |
| ⚠️ **DEGRADED** (รันได้แต่ไม่ครบเงื่อนไข) | 0 |
| ⬜ **ยังไม่ได้รัน** | **124** |
| **รวม** | **124** |

> 🔴 **ผลรวมต้องเท่ากับ 124 เป๊ะเสมอ** — ถ้าไม่เท่า แปลว่ามีเคสหายไปจากการนับ ไม่ใช่เรื่องปัดเศษ

---

## 3. ผลรายเคส

**ค่าที่ใช้ได้ในช่องผล:** `PASS` · `FAIL` · `BLOCKED` · `DEGRADED`

| TC | Req ID | Pri | ผล | รอบที่ | หลักฐาน | BUG | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| — | *(ยังไม่มี)* | | | | | | |

**กติกาการกรอก:**

| สถานการณ์ | ต้องทำอะไร |
|---|---|
| ผลตรง **ทุกข้อ** ของ "ผลที่คาดหวัง" | `PASS` |
| ผลตรง **บางข้อ** | ❌ **ไม่ใช่ PASS** — เป็น `FAIL` และเขียนว่าข้อไหนไม่ตรง |
| รันไม่ได้เพราะของยังไม่มี/อุปกรณ์ไม่ครบ | `BLOCKED` + ระบุว่าติดอะไรและใครปลดล็อก |
| รันได้แต่เงื่อนไขไม่ครบจริง | `DEGRADED` + เขียนว่าขาดอะไร · **ห้ามใช้อ้างว่า NFR ที่เกี่ยวข้องผ่าน** |
| `FAIL` | ต้องมี `BUG-xxx` ใน [[bug-log-v1\|Bug Log v1]] ทันที ไม่มีข้อยกเว้น |
| เคส `U` (usability) ที่หาผู้ทดสอบไม่ครบ | รายงานเป็น **indicative** ห้ามรายงานเป็น % ที่ผ่านเกณฑ์ |

---

## 4. ผล UAT

ตาม [[../01-test-plan/uat-scenario-v1|UAT Scenario v1]] — แยกจากผล test case เพราะวัดคนละอย่าง

| UAT | ผู้ทดสอบ (กี่คน) | สำเร็จ / ทั้งหมด | เข้าใจถูก / ทั้งหมด | ผ่านเกณฑ์ | คำพูดดิบที่ควรจำ |
|---|---|---|---|---|---|
| — | *(ยังไม่มี)* | | | | |

> **จดคำพูดดิบของผู้ทดสอบไว้เสมอ** โดยเฉพาะประโยคที่แสดงความเข้าใจผิดเรื่องการจ่ายเงิน — เป็นวัตถุดิบที่ [[../../04-retrospectives/index|04-retrospectives]] ใช้ได้จริง และหาย่อนหลังไม่ได้ถ้าไม่จดตอนนั้น

---

## 5. เคสที่ยังรันไม่ได้ ณ วันนี้

ยกมาจาก [[../01-test-plan/test-plan-v1|Test Plan v1]] §5 — **18 เคส** ที่รู้ล่วงหน้าแล้วว่าจะรันไม่ได้

| กลุ่ม | เคส | จำนวน | ปลดล็อกโดย |
|---|---|---|---|
| **G-1** เลขคิว + สถานะ "รอเสิร์ฟ" ยังไม่มีใน schema | TC-052…055 · TC-055a/b · TC-114 · TC-115 | 8 | COULSON — **D-2** |
| **G-2** ป้าย QR ยังไม่ถูกออกแบบ | TC-049…051 | 3 | SHURI + COULSON — **D-1/D-2** |
| **G-3** auth ยังไม่รองรับ NFR-06/10/11 | TC-062 · TC-063 · TC-066 · TC-067 | 4 | COULSON — **D-3** |
| **degraded** ต้องใช้บัญชีเดียวหลายเครื่อง | TC-059 · TC-101 · TC-102 | 3 | COULSON — **D-3** |

🔴 **ทั้งสี่กลุ่มอยู่ใน exit criteria ของ Phase 0** — ตราบใดที่ยังรันไม่ได้ **ห้ามรายงานว่า Phase 0 ผ่านการทดสอบ** ไม่ว่าเคสที่เหลือจะ PASS ครบแค่ไหน
