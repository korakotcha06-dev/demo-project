# Test Result v1 — Phase 0 (MVP)

> ~~**สถานะ: ยังไม่มีใครทดสอบแม้แต่เคสเดียว** (สร้างโครง 2026-08-16)~~
> **อัปเดต 2026-08-28: รันชุดเทสอัตโนมัติจริงรอบแรกแล้ว** — ดูตัวเลขที่ §2 · เอกสารนี้ยังไม่ใช่ GO/NO-GO ฉบับเต็ม เพราะ 75/124 เคสยังไม่มีเทสอัตโนมัติครอบ (ต้องลงมือทดสอบด้วยมือ) และ 9/124 เคสยังทดสอบไม่ได้จริงในสภาพแวดล้อมตอนนี้ — รายละเอียดที่ §3 และ §5
> เจ้าของ: **OKOYE** · กรอกด้วย `/test-run` เท่านั้น
> เคสต้นทาง: [[../01-test-plan/test-case-v1|Test Case Specification v1]] (124 เคส) · เกณฑ์ผ่าน: [[../01-test-plan/test-plan-v1|Test Plan v1]] §7

> ~~🔴 **ห้ามกรอกไฟล์นี้ล่วงหน้า** — ทุกแถวต้องมาจากการลงมือรันจริงกับระบบที่ทำงานได้ · ตอนนี้ยังไม่มีโค้ดแอปพลิเคชันในโปรเจกต์เลย~~ (ล้าสมัยแล้ว — โค้ดมีแล้วตั้งแต่ 2026-08-22 ดู [[../../../CLAUDE|CLAUDE.md]]) · **กติกาเดิมยังใช้อยู่**: ทุกแถวด้านล่างมาจากการรันจริงหรือจากการอ่านโค้ดจริงเทียบกับเคส ไม่มีแถวไหนเดา

กลับไปที่ [[index|02-test-result]]

---

## 1. รอบการทดสอบ

| รอบ | วันที่ | ผู้ทดสอบ | build / commit | สภาพแวดล้อม | test data |
|---|---|---|---|---|---|
| 1 | 2026-08-28 07:58 | Touch (เครื่องส่วนตัว) | ⚠️ **ไม่ได้บันทึก commit sha ไว้ตอนรัน** — งานนี้ถูกสั่งห้ามรันคำสั่ง `git` จึงยืนยันย้อนหลังไม่ได้ ต้องแก้ในรอบถัดไป | Node + `pnpm test` (= `vitest run`) ยิงใส่ PostgreSQL **local** ฐาน `qr_order_test` จริง (ไม่ใช่ mock) | fixture ที่ `tests/helpers.ts` สร้างเองต่อไฟล์ผ่าน `prepareTestDatabase()`/`resetTransactionalData()` (ร้าน A + ร้าน B จำลอง multi-tenant) — **ไม่ใช่** ข้อมูลจาก `pnpm seed` ของ dev |

**ผลรันดิบ:** `Test Files 20 passed (20)` · `Tests 268 passed (268)` · เวลารวม 31.11s · exit code 0 · ไม่มีเคสแดง ไม่มีเคส skip · รายชื่อเคสทั้งหมด 268 รายการถูกใช้เป็นหลักฐานอ้างอิงในตาราง §3

> ⚠️ **ห้ามเริ่มรอบใหม่โดยไม่บันทึก build/commit** — ผลทดสอบที่ไม่รู้ว่าทดสอบอะไรอยู่ ไม่มีค่าเป็นหลักฐาน **รอบที่ 1 นี้ทำผิดกติกาข้อนี้ไปแล้วเรื่อง commit sha — ผู้รันรอบถัดไปต้องบันทึกให้ครบ**

> 🔴 **ข้อจำกัดสำคัญของรอบนี้ที่ต้องอ่านก่อนเชื่อตัวเลขใน §2–§3**: ชุดเทสอัตโนมัติทั้งหมดเป็น **integration test ระดับ backend/DB เท่านั้น** (`tests/integration/*.test.ts` เรียกฟังก์ชัน server ตรง ๆ + 1 unit test) — **ไม่มีเทส frontend/component (.test.tsx) และไม่มี e2e/browser test (Playwright/Cypress) อยู่ในโปรเจกต์เลย** (ยืนยันจาก `package.json` และการค้นไฟล์) ดังนั้นเคสที่วัดผลจากสิ่งที่ "ตาเห็นบนจอ" ล้วน ๆ — จำนวน tap, เวลาที่ลูกค้ารู้สึก, ป้าย/ไอคอนบนจอ, การไม่กดรีเฟรชแล้วเห็นการเปลี่ยนแปลง, การล็อกอินจริงหลายเครื่อง — **ไม่มีทางถูกเทสอัตโนมัติชุดนี้ครอบได้ไม่ว่าตรรกะฝั่งเซิร์ฟเวอร์จะถูกแค่ไหน** ต้องทดสอบด้วยมือเสมอ

---

## 2. สรุปตัวเลขรอบล่าสุด

**หมายเหตุการตีความ:** ตารางนี้คือผลจากการรันเทสอัตโนมัติจริงรอบที่ 1 (§1) ผสมกับการอ่านโค้ดจริงเทียบกับเคสสำหรับเคสที่ไม่มีเทสอัตโนมัติ — **ไม่ใช่ผล GO/NO-GO ของ Phase 0** เพราะเคสที่สำคัญมาก (usability, error-path ฝั่ง UI, NFR ที่วัดเวลา) ส่วนใหญ่ยังไม่ถูกทดสอบเลยไม่ว่าจะด้วยมือหรืออัตโนมัติ

| สถานะ | จำนวน | ความหมายในรอบนี้ |
|---|---|---|
| ✅ **PASS** | 40 | มีเทสอัตโนมัติที่รันผ่านจริงในรอบที่ 1 ครอบเกณฑ์หลักของเคสนั้น |
| ❌ **FAIL** | 0 | ไม่มีเคสอัตโนมัติเคสไหนแดงในรอบนี้ |
| 🔒 **BLOCKED** | 9 | ทดสอบไม่ได้จริงในสภาพแวดล้อมตอนนี้ — แยกเป็น 2 กลุ่ม (5 เคสเพราะขาดอุปกรณ์จริง/เครื่องพิมพ์ · 4 เคสเพราะฟีเจอร์ในเอกสารยังไม่ถูกสร้างในโค้ด) ดู §5 |
| ⚠️ **DEGRADED** | 0 | — |
| ⬜ **ยังไม่ได้รัน** (= ยังไม่มีเทสอัตโนมัติครอบ ต้องทดสอบด้วยมือ) | 75 | ส่วนใหญ่เป็นเคส UI/เบราว์เซอร์/ผู้ทดสอบจริง/อุปกรณ์จริงหลายเครื่อง ที่ระบบเทสปัจจุบัน (integration+unit เท่านั้น) ครอบไม่ถึง |
| **รวม** | **124** | |

> 🔴 **ผลรวมต้องเท่ากับ 124 เป๊ะเสมอ** — ถ้าไม่เท่า แปลว่ามีเคสหายไปจากการนับ ไม่ใช่เรื่องปัดเศษ ✅ ตรวจแล้ว: 40+0+9+0+75 = 124

**สัดส่วนความครอบคลุมด้วยเทสอัตโนมัติ:** 40/124 = **32.3%** ผ่านแล้วจริง · 9/124 = 7.3% ทดสอบไม่ได้ตอนนี้ · 75/124 = **60.5%** ยังต้องลงมือทดสอบด้วยมือก่อนจะสรุป GO/NO-GO ได้

---

## 3. ผลรายเคส

**ค่าที่ใช้ได้ในช่องผล:** `PASS` · `FAIL` · `BLOCKED` · `DEGRADED` · `ยังไม่ได้รัน` (= ยังไม่มีเทสอัตโนมัติครอบ ต้องทดสอบด้วยมือ)
**คอลัมน์ "หมายเหตุ"** สำหรับแถว PASS จะระบุไฟล์เทส+ชื่อเคสที่รองรับเสมอ ตามกติกา "ห้ามแมปแบบเดา" — ถ้าไม่มั่นใจว่าเทสไหนครอบตรง ให้ลงเป็น `ยังไม่ได้รัน` แทน แม้จะมีเทสใกล้เคียงอยู่ก็ตาม (จะระบุเทสใกล้เคียงไว้เป็นข้อมูลประกอบ ไม่ใช่หลักฐาน PASS)

### S1 — ลูกค้า: เส้นทางหลัก (TC-001…014)

> ทั้งหมดในหมวดนี้เป็นพฤติกรรมที่ต้องเห็นบนจอจริง (สแกน QR, จำนวน tap, การเปลี่ยนหน้า) — ไม่มีเทส frontend/e2e ในระบบเลย จึงไม่มีเคสไหนใน S1 ที่เทสอัตโนมัติชุดนี้ครอบได้

| TC | Req ID | Pri | ผล | รอบที่ | หลักฐาน | BUG | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| TC-001 | US-01 · BR-10 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ (สแกน QR จริง + นับหน้าที่คั่น) |
| TC-002 | US-01 · BR-10 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-003 | US-02 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ (นับการ์ดบนจอจริง) |
| TC-004 | US-02 · F-2 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-005 | US-02 · D-3 | P1 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-006 | US-03 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ (นับ tap) |
| TC-007 | US-03 · UX-03 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-008 | US-04 · BR-11 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-009 | US-04 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-010 | US-05 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ (ต้องดูว่ากดรีเฟรชหรือไม่) |
| TC-011 | US-06 · BR-1 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-012 | US-06 | P1 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-013 | US-07 · UX-12 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-014 | US-28 · BR-12 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือสำหรับส่วนหน้าจอ (ป้ายช่องทาง, ไม่มีปุ่ม "จ่ายทีหลัง") — **ส่วน backend ผ่านแล้วจริง**: `tests/integration/counter.test.ts` > "🔴 ออเดอร์เข้าสถานะ awaiting_payment — ยังไม่เข้าคิวเตรียมของ" ยืนยันว่าไม่ขึ้นคิวเตรียมของและสถานะถูกต้อง |

### S2 — พนักงานหน้าร้าน (TC-015…032)

| TC | Req ID | Pri | ผล | รอบที่ | หลักฐาน | BUG | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| TC-015 | US-13 · UX-10 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ (ไม่กดรีเฟรช, ป้ายไอคอน) |
| TC-015a | US-15 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ (เสียง+จอดับ, ใช้อุปกรณ์จริง) — ไม่ใช่ BLOCKED เพราะทำได้ถ้ามีคนทดสอบ |
| TC-015b | US-15 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-016 | US-14 | P0 | ยังไม่ได้รัน | — | — | — | กลไก transition สถานะถูกเทสแล้วที่ `tests/integration/concurrency.test.ts` §5.4 แต่ **ไม่มีเทสยืนยันว่า label เริ่มต้นของช่องทางโต๊ะคือ "รอดำเนินการ"** และไม่มีเทส NFR-02 (ลูกค้าเห็นโดยไม่รีเฟรช) — คงเป็นต้องทดสอบด้วยมือ |
| TC-016a | US-14 · US-30 | P0 | **BLOCKED** | 1 | อ่านโค้ดจริง 2026-08-28: `src/server/order-views.ts:16` และ `src/server/staff/orders.ts:22` มี label เดียวคือ `queued: 'รอดำเนินการ'` — **ไม่มีค่า/label "รอเสิร์ฟ" อยู่ในโค้ดเลยแม้แต่จุดเดียว** (`grep -rn "รอเสิร์ฟ" src/ tests/` = 0 ผลลัพธ์) และ `src/server/staff/counter.ts:172` ตั้งสถานะออเดอร์เคาน์เตอร์หลังจ่ายเงินเป็น `'queued'` เหมือนช่องทางโต๊ะทุกประการ | — | **คำตัดสินของ Touch เมื่อ 2026-08-16 ("รอเสิร์ฟ เป็น status field") ยังไม่ถูก implement** เหมือนกับ TC-114/115 — ดู §5 |
| TC-017 | US-14 · BR-25 | P0 | **BLOCKED** | 1 | เหมือน TC-016a — ไม่มีสถานะ "รอเสิร์ฟ" ให้เดินผ่านเลย | — | เดียวกับ TC-016a |
| TC-018 | US-14 · US-30 | P1 | **BLOCKED** | 1 | เหมือน TC-016a — ไม่มีสถานะ "รอเสิร์ฟ" อยู่ในระบบเลยไม่ว่าช่องทางไหน จึงพิสูจน์ "ไม่มีให้เลือกในช่องทางโต๊ะ" แบบมีความหมายไม่ได้ (เพราะไม่มีให้เลือกในช่องทางไหนเลย) | — | เดียวกับ TC-016a |
| TC-019 | US-16 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ (label "หมดชั่วคราว" + ไอคอน บนจอ) |
| TC-020 | US-16 · D-1 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-021 | US-18 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ (เลย์เอาต์จอ) |
| TC-022 | US-18 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ (ยอดรวม 3 รอบเทียบเครื่องคิดเลข) |
| TC-023 | US-19 · BR-11 | P0 | **PASS** | 1 | `tests/integration/invariants.test.ts` > "D7 — service_point ไม่มีคอลัมน์ status" > "อ่านสถานะได้จาก view เท่านั้น และเปลี่ยนตามวงจรชีวิตออเดอร์" + `tests/integration/multi-device.test.ts` > "BR ข้อ 7" > "ลูกค้าชุดใหม่ที่สแกนหลังปิดบิล เริ่มรอบใหม่ได้และเห็นออเดอร์เป็นศูนย์" | — | สถานะโต๊ะ derive จาก view จริง ยืนยันแล้ว |
| TC-024 | US-19 · BR-7 | P0 | **PASS** | 1 | `tests/integration/multi-device.test.ts` > "BR ข้อ 7 — ปิดบิลแล้ว session ของทุกอุปกรณ์ตายทันที" > "ลูกค้าชุดใหม่ที่สแกนหลังปิดบิล เริ่มรอบใหม่ได้และเห็นออเดอร์เป็นศูนย์" | — | — |
| TC-025 | US-19 · BR-11 | P0 | ยังไม่ได้รัน | — | — | — | ต้องตรวจด้วยมือ (ไล่หาปุ่มทุกหน้าจอ — เป็นการพิสูจน์ "ไม่มี" ซึ่งเทสอัตโนมัติไม่ครอบ) |
| TC-026 | US-20 · BR-5 | P0 | **PASS** | 1 | `tests/integration/invariants.test.ts` > "INV-6 — ยกเลิกต้องมีเหตุผลเสมอ" > "ตั้ง status=cancelled โดยไม่มี cancel_reason ต้องถูกปฏิเสธ" + "เหตุผลที่เป็นช่องว่างล้วนก็ต้องถูกปฏิเสธ" | — | — |
| TC-027 | US-20 | P0 | ยังไม่ได้รัน | — | — | — | ไม่พบเทสที่ยกเลิกเฉพาะ 1 รายการแล้วตรวจยอดบิลคำนวณใหม่โดยตรง — ต้องทดสอบด้วยมือ |
| TC-028 | US-29 · BR-12 | P0 | **PASS** | 1 | `tests/integration/counter.test.ts` > "US-29 คิวรอยืนยันการชำระเงิน (S-03)" > "🔴 คิวนี้แยกจากคิวโต๊ะ dine-in — ออเดอร์โต๊ะต้องไม่โผล่มา" | — | — |
| TC-029 | US-29 | P0 | **PASS** | 1 | `tests/integration/counter.test.ts` > "🔴 กดรับเงินแล้วออเดอร์เข้าคิวเตรียมของทันที + บิลเป็น paid" + "บันทึก payment 1 แถว และมีร่องรอยใน order_status_event" | — | — |
| TC-030 | US-30 | P0 | **PASS** | 1 | `tests/integration/counter.test.ts` > "US-30 คีย์ออเดอร์แทนลูกค้า (S-04)" > "🔴 รับเงินอยู่ในขั้นตอนเดียวกัน → ออเดอร์เข้าคิวทันที ไม่ต้องมายืนยันทีหลัง" + "ไม่แตะตะกร้าเลย — ลูกค้าไม่ต้องมีอุปกรณ์/ไม่ต้องสแกน QR" | — | — |
| TC-031 | US-30 | P0 | **PASS** | 1 | `tests/integration/counter.test.ts` > "คีย์ต่อเนื่องหลายลูกค้าติดกัน → ออเดอร์แยกกันไม่ปน" | — | — |
| TC-032 | US-30 · BR-12 | P1 | **BLOCKED** | 1 | ครึ่งแรก ("อยู่คิวเดียวกัน") มีเทสรองรับ (`counter.test.ts` > "ใช้เลขคิวชุดเดียวกับออเดอร์ที่ลูกค้าสั่งเอง — ไม่แยกสองชุด") แต่ครึ่งหลัง ("ทั้งคู่ต้องอยู่ในสถานะ 'รอเสิร์ฟ' เหมือนกัน") ทำไม่ได้เพราะไม่มีสถานะนี้ในโค้ด (เหมือน TC-016a) | — | เกณฑ์ผ่านต้องผ่าน**ทั้งสองข้อ** ข้อสองยังเป็นไปไม่ได้จริง |

### S3 — แอดมิน (TC-033…048)

| TC | Req ID | Pri | ผล | รอบที่ | หลักฐาน | BUG | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| TC-033 | US-23 | P0 | **PASS** | 1 | `tests/integration/admin.test.ts` > "A-03 จัดการเมนู — US-43" > "เพิ่มเครื่องดื่มแล้วลูกค้าเห็นทันที พร้อมตัวเลือกเท่าที่เลือกไว้จริง" | — | เทสเรียก endpoint เมนูฝั่งลูกค้าโดยตรง ไม่ใช่เปิดเบราว์เซอร์จริง แต่พิสูจน์ประเด็นหลัก (ไม่ต้อง deploy ใหม่) ได้ |
| TC-034 | US-23 · F-2 | P1 | ยังไม่ได้รัน | — | — | — | ไม่พบเทสตรวจการแยกแยะ 2 รายการลาเต้ในหน้าจัดการเมนู — ต้องทดสอบด้วยมือ |
| TC-035 | US-24 · BR-10 | P0 | **PASS** | 1 | `tests/integration/admin.test.ts` > "A-06 จัดการโต๊ะ — US-42" > "โต๊ะใหม่ได้ QR ของตัวเองที่ไม่ซ้ำใคร และใช้งานได้ทันที" | — | — |
| TC-036 | US-24 · BR-10 | P1 | ยังไม่ได้รัน | — | — | — | เป็นการพิสูจน์ "ไม่มีฟังก์ชันนี้อยู่จริง" ทั่วทั้งระบบ — ต้องตรวจด้วยมือ/ไล่โค้ด ไม่ใช่เทสอัตโนมัติเดี่ยว |
| TC-037 | US-42 | P0 | **PASS** | 1 | `tests/integration/admin.test.ts` > "โต๊ะใหม่ได้ QR ของตัวเองที่ไม่ซ้ำใคร และใช้งานได้ทันที" | — | เทสเดียวกับ TC-035 ครอบทั้งสองแง่มุม |
| TC-038 | US-42 | P0 | **PASS** | 1 | `tests/integration/admin.test.ts` > "🔴 เปลี่ยนชื่อโต๊ะหลายรอบ QR เดิมยังใช้ได้ (สัญญาถาวรของ QR)" | — | — |
| TC-039 | US-42 · D-6 | P0 | **PASS** | 1 | `tests/integration/admin.test.ts` > "ปลดระวางโต๊ะที่มีบิลเปิด → บล็อก พร้อมบอกว่าต้องปิดบิลก่อน" | — | — |
| TC-040 | US-42 | P0 | ยังไม่ได้รัน | — | — | — | ครึ่งหลัง ("โต๊ะปลดระวางแล้วสั่งใหม่ไม่ได้") มีเทสรองรับที่ `table-orders.test.ts` > "โต๊ะที่เอาออกจากผังแล้ว รับออเดอร์ใหม่ไม่ได้" แต่ครึ่งแรก ("ประวัติออเดอร์เก่ายังเรียกดูได้ครบ") ไม่พบเทสตรง — ทั้งเคสจึงยังไม่ได้รัน |
| TC-041 | US-35 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ (แสดงผลบนจอ) |
| TC-042 | US-35 | P0 | ยังไม่ได้รัน | — | — | — | ไม่พบเทสตรวจค่า default "กลาง" โดยตรง — ต้องทดสอบด้วยมือ |
| TC-043 | US-35 · US-36 | P0 | ยังไม่ได้รัน | — | — | — | มีเทสใกล้เคียงที่ `admin.test.ts` > "A-05 เมล็ดพิเศษ" > "ปิดเมล็ดพิเศษทุกตัว → ลูกค้ายังเห็นกลุ่ม แต่เหลือแค่เมล็ดของร้าน" แต่ **คำอธิบายของเทสนั้นดูขัดกับเกณฑ์ของ TC-043 ("กลุ่มหายไปทั้งกลุ่ม")** — ไม่มั่นใจว่าครอบตรงกัน จึงลงเป็นยังไม่ได้รันตามกติกา "ห้ามแมปแบบเดา" — ⚠️ ควรให้ OKOYE ไล่โค้ดอีกรอบเพื่อยืนยันว่าไม่ใช่ความขัดแย้งจริง |
| TC-044 | US-36 | P0 | ยังไม่ได้รัน | — | — | — | กลไก toggle ถูกเทสในบริบทอื่นแล้ว (A-05) แต่ยังไม่มีเทสตรงสำหรับ "เกอิชา" ตัวเดียว+มุมมองลูกค้า — ต้องทดสอบด้วยมือ |
| TC-045 | US-43 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ (แสดงผลบนจอ) |
| TC-046 | US-43 · D-5 | P1 | ยังไม่ได้รัน | — | — | — | มีเทสใกล้เคียงคนละประเด็น (`option-prices.test.ts` > "ความหวานไม่โผล่ในหน้าเปิด-ปิดขาย" คือฝั่งแอดมิน ไม่ใช่ฝั่งหน้าสินค้าลูกค้า) — ต้องทดสอบด้วยมือ |
| TC-047 | US-35 · US-43 | P0 | ยังไม่ได้รัน | — | — | — | กลไก snapshot ตัวเลือกบนตั๋วถูกเทสในบริบทอื่น (`counter.test.ts` > "snapshot ชื่อตัวเลือกไว้...") แต่ไม่ตรงกับคู่ตัวเลือก คั่ว+หวาน ของ TC-047 — ต้องทดสอบด้วยมือ |
| TC-048 | US-43 | P1 | ยังไม่ได้รัน | — | — | — | ไม่พบเทสถอดกลุ่มตัวเลือกจากสินค้ารายตัว — ต้องทดสอบด้วยมือ |

### S4 — ป้าย QR ทางกายภาพ (TC-049…051)

> 🟢 **อัปเดตสำคัญ**: กลุ่มนี้เดิมถูกระบุว่า "รันไม่ได้" ทั้งหมด (GAP G-2) — จากการอ่านโค้ดจริงวันนี้ **G-2 ถูกปิดแล้ว** โค้ดมี `src/server/admin/qr-signage.ts` ที่ render ป้ายเต็มใบจริง (ไม่ใช่ QR เปล่า) และมีเทสอัตโนมัติรองรับ 2 ใน 3 เคส

| TC | Req ID | Pri | ผล | รอบที่ | หลักฐาน | BUG | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| TC-049 | US-44 · US-24 | P0 | **PASS** | 1 | `tests/integration/admin.test.ts` > "A-07 ป้าย QR พร้อมพิมพ์" > "ป้ายมีองค์ประกอบครบตาม US-44 และบอกว่าไม่ต้องติดตั้งแอป" + `tests/integration/qr-signage-raster.test.ts` > "🔴 PNG เป็นป้ายเต็มใบขนาด A6 ที่ 300dpi ไม่ใช่ QR เปล่า" | — | GAP G-2 (ป้ายเป็น QR เปล่า) ปิดแล้ว |
| TC-050 | US-44 · BR-24 | P0 | **PASS** | 1 | `tests/integration/admin.test.ts` > "ป้ายเคาน์เตอร์บอกกฎการจ่ายเงินของช่องทางตัวเอง ไม่ใช่ของ dine-in" | — | — |
| TC-051 | US-44 | P0 | **BLOCKED** | 1 | ไม่มีเครื่องพิมพ์จริงในสภาพแวดล้อมทดสอบวันนี้ — ต้องพิมพ์ป้ายออกมาจริงแล้ววัดด้วยไม้บรรทัด + สแกนจริง 5 ครั้งจากระยะ 1 เมตร | — | ตัวเลข mm/dpi ผ่านการคำนวณอัตโนมัติแล้ว (ดู TC-049) แต่การวัด/สแกนจริงยังทำไม่ได้จนกว่าจะมีเครื่องพิมพ์ |

### S5 — เลขคิวช่องทางเคาน์เตอร์ (TC-052…055)

> 🟢 **อัปเดตสำคัญ**: กลุ่มนี้เดิมถูกระบุว่า "รันไม่ได้" ทั้งหมด (GAP G-1 — ไม่มีคอลัมน์เลขคิว) — จากการอ่านโค้ด+เทสวันนี้ **ครึ่ง "เลขคิว" ของ G-1 ถูกปิดแล้ว** (migration `015_queue_no_customer_label.sql` เพิ่มคอลัมน์ `queue_no` พร้อม constraint ครบ) แต่ **ครึ่ง "สถานะรอเสิร์ฟ" ของ G-1 ยังไม่ถูกปิด** — ดู TC-114/115 ที่ยังคง BLOCKED

| TC | Req ID | Pri | ผล | รอบที่ | หลักฐาน | BUG | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| TC-052 | US-45 · BR-25 | P0 | **PASS** | 1 | `tests/integration/counter.test.ts` > "US-28..." > "ได้เลขคิวรูปแบบ A01 และบิลเปิดในสถานะรอชำระ" | — | ส่วน "แสดงเด่นที่สุดบนจอ" เป็น UI ยังไม่ถูกเทส |
| TC-053 | US-45 | P0 | **PASS** | 1 | `tests/integration/counter.test.ts` — ค่า `queue_no` ถูกอ่าน/เทียบตรงกันทั้งจากออเดอร์และคิวพนักงาน (`row.queueNo` ตรงกับ `res.order.queue_no` ในหลายเคส) | — | ยืนยันว่าเลขเดียวกันในทุก view ที่มาจาก field เดียวกันในฐานข้อมูล |
| TC-054 | US-45 · BR-25 | P1 | **PASS** | 1 | `tests/integration/counter.test.ts` > "US-45 เลขคิว A01" > "🔴 ช่องทางโต๊ะไม่มีเลขคิว — ใช้เลขโต๊ะอยู่แล้ว (BR ข้อ 25)" + "ฐานข้อมูลปฏิเสธเลขคิวบนออเดอร์ช่องทางโต๊ะ (ไม่พึ่งวินัยของโค้ด)" | — | บังคับทั้งระดับโค้ดและ DB constraint |
| TC-055a | US-45 | P0 | **BLOCKED** | 1 | ไม่มีเครื่องพิมพ์ใบเสร็จ/สลิปจริงในสภาพแวดล้อมทดสอบ | — | ตรงตามที่เอกสารต้นฉบับกำหนดไว้เองว่าให้รายงานแบบนี้ถ้าไม่มีเครื่องพิมพ์ |
| TC-055b | US-45 · US-30 | P0 | ยังไม่ได้รัน | — | — | — | เป็นการทดสอบปฏิสัมพันธ์จริงกับลูกค้า/พนักงาน — ต้องทดสอบด้วยมือ (เลขคิวเองมีเทสรองรับแล้วที่ TC-052-054) |
| TC-055 | US-45 · NFR-06 | P0 | ยังไม่ได้รัน | — | — | — | มีเทสยิงคำสั่งต่อเนื่อง (ไม่ใช่พร้อมกันจริง) ยืนยันเลขเดินไม่ซ้ำ (`"เดินเลขต่อกันไม่ซ้ำ A01 → A02 → A03"`) และมี unique index กันชนที่ระดับ DB แต่ **ไม่มีเทสที่ยิง 4 คำขอพร้อมกันจริงแบบเคสนี้ต้องการ** — ลงเป็นยังไม่ได้รันตามกติกาความซื่อสัตย์ |

### S6 — NFR (TC-056…073)

| TC | Req ID | Pri | ผล | รอบที่ | หลักฐาน | BUG | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| TC-056 | NFR-01 · UX-01 | P0 | ยังไม่ได้รัน | — | — | — | ต้องวัดเวลาจริงบนเบราว์เซอร์+เน็ต 4G |
| TC-057 | NFR-01 | P0 | ยังไม่ได้รัน | — | — | — | ต้องวัดเวลาจริงพร้อม throttle |
| TC-058 | NFR-02 · UX-04 | P0 | ยังไม่ได้รัน | — | — | — | ทดสอบ realtime ฝั่ง UI จริง |
| TC-059 | NFR-03 | P0 | ยังไม่ได้รัน | — | — | — | เดิมอยู่กลุ่ม "degraded ต้องใช้บัญชีเดียวหลายเครื่อง" — ยังต้องทดสอบด้วยมือ (เป็นเรื่อง timing ของหน้าจอจริงหลายเครื่อง ไม่ใช่ฟีเจอร์ขาด) |
| TC-060 | NFR-04 | P0 | ยังไม่ได้รัน | — | — | — | การอัปเดตไม่ต้อง deploy ถูกพิสูจน์ระดับ backend แล้ว (ดู TC-033) แต่การวัดเวลา ≤5 วินาทีบนจอลูกค้าจริงยังไม่ถูกเทส |
| TC-061 | NFR-05 · UX-06 · BR-2 | P0 | **PASS** | 1 | `tests/integration/concurrency.test.ts` > "§5.1" > "2 อุปกรณ์เพิ่มเครื่องดื่มเดียวกันพร้อมกัน → รวมจำนวน ไม่มีรายการหาย (0% data loss)" + "5 อุปกรณ์เพิ่มพร้อมกัน → qty = 5 ครบ ไม่หายแม้แถวเดียว" | — | ครอบเฉพาะส่วน "0% data loss" — ส่วน "เห็นภายใน ≤5 วินาที" (UI) ยังไม่ถูกเทส |
| TC-062 | **NFR-06** · BR-26 | **P0** | ยังไม่ได้รัน | — | — | — | โค้ดมีคอมเมนต์ยืนยันเจตนา (`src/server/staff/auth.ts:11` "NFR-06 ถูกปิดด้วยการ *ไม่ทำ* ฟีเจอร์เด้งเครื่องเก่า") แต่ไม่มีเทสอัตโนมัติล็อกอิน 4 เครื่องจริงยืนยัน — ต้องทดสอบด้วยมือ |
| TC-063 | NFR-07 · BR-26 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบ realtime ข้าม 4 อุปกรณ์จริง |
| TC-064 | NFR-08 · BR-4 | P0 | **PASS** | 1 | `tests/integration/concurrency.test.ts` > "§5.2" > "สินค้าหมดตอนกดยืนยัน → reject รายการนั้นรายการเดียว บิลไม่ล้ม (HTTP 201)" | — | — |
| TC-065 | NFR-09 | P0 | **PASS** | 1 | `tests/integration/concurrency.test.ts` > "§5.3" > "ยอดเปลี่ยนไปแล้ว → 409 BILL_CHANGED พร้อมยอดใหม่ ห้ามปิดเงียบ ๆ" + "สั่งเพิ่มหลังบิลปิด → 409 SESSION_CLOSED และตะกร้าไม่หาย" | — | — |
| TC-066 | **NFR-10** · BR-27 | **P0** | ยังไม่ได้รัน | — | — | — | โค้ดมี logic รองรับแล้ว (`src/server/staff/auth.ts` เงื่อนไข `EXISTS` ไม่หมดอายุถ้ามีออเดอร์ค้าง) แต่ไม่มีเทสจำลองผ่านไป ≥12 ชั่วโมงจริง — ต้องทดสอบด้วยมือ/เทสเพิ่ม |
| TC-067 | **NFR-11** · BR-27 | **P0** | **PASS** | 1 | `tests/integration/station-lock.test.ts` > "ปลดล็อกด้วย PIN" > "PIN ถูก → ปลดล็อก และเสร็จภายใน 3 วินาที (NFR-11)" | — | GAP G-3 ส่วน NFR-11 ปิดแล้ว |
| TC-068 | NFR-12 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบตัดเน็ตจริงบนอุปกรณ์จริง |
| TC-069 | NFR-13 | P1 | ยังไม่ได้รัน | — | — | — | โค้ดกรองสถานะ `completed` ออกจากคิวงานแล้ว (`ACTIVE_STATUSES` ใน `src/server/staff/orders.ts`) แต่ไม่พบเทสยืนยันจำนวนใบบนจอลดลงหลังกด "ส่งมอบแล้ว" โดยตรง — ต้องทดสอบด้วยมือ |
| TC-070 | NFR-14 | P0 | **PASS** | 1 | `tests/integration/role-gate.test.ts` > "BR ข้อ 28..." > "ไม่มี cookie → ถูกปฏิเสธทั้งสองด่าน" + "ทุก route ใต้ /api/v1/admin เรียก requireAdmin — ไม่มี endpoint ที่ลืมใส่ด่าน" | — | ครอบเฉพาะการปฏิเสธที่ระดับ API — การ "ส่งไปหน้าล็อกอิน" (UI redirect) ยังไม่ถูกเทส |
| TC-071 | NFR-15 | P0 | ยังไม่ได้รัน | — | — | — | มีเทสรั่วข้าม**ร้าน**ครบ (`tenant-isolation.test.ts`) แต่ไม่พบเทสตรงสำหรับใช้ token ของโต๊ะหนึ่งเรียกตะกร้าอีกโต๊ะ **ในร้านเดียวกัน** ตามที่ TC-071 ระบุ — ต้องทดสอบด้วยมือ/เทสเพิ่ม |
| TC-072 | NFR-17 · BR-14 | P0 | ยังไม่ได้รัน | — | — | — | เป็นการตรวจ schema ทั้งฐาน (audit) ไม่ใช่รูปแบบเทสอัตโนมัติทั่วไป — ต้องตรวจด้วยมือ |
| TC-072a | NFR-17 · BR-14 | P0 | ยังไม่ได้รัน | — | — | — | เป็น UI (ปุ่มต้องกดได้ตอนช่องว่าง) — ต้องทดสอบด้วยมือ · ⚠️ เอกสารเดิมระบุว่าโปรโตไทป์เคย **FAIL** เคสนี้ (ปุ่มถูก disable) ยังไม่ยืนยันว่าแก้แล้วหรือยัง — ดู "ต้องคิดต่อ" |
| TC-072b | NFR-17 · BR-14 | P0 | ยังไม่ได้รัน | — | — | — | เป็นการตรวจ schema/query หาความเชื่อมโยง — ต้องตรวจด้วยมือ |
| TC-072c | NFR-17 · BR-14 | P0 | **BLOCKED** | 1 | ค้นทั้งโค้ด (`grep -rn "90 \|purge\|retention"`) **ไม่พบ job ล้างชื่อเล่นอายุ 90 วันอยู่ในโค้ดเลย** | — | ฟีเจอร์ที่เคสนี้ต้องใช้ยังไม่ถูกสร้าง — ไม่ใช่แค่ยังไม่ได้ทดสอบ |
| TC-073 | NFR-19 · BR-23 | P0 | ยังไม่ได้รัน | — | — | — | ต้องวัด contrast/ขนาดจริงบนหน้าจอทั้ง 7 หน้า |

### S6b — Usability ที่วัดผลได้ (TC-074…081)

> ทุกเคสในหมวดนี้เป็นชนิด `U` ต้องใช้ผู้ทดสอบจริงหลายคน — ไม่มีทางเทสอัตโนมัติได้ และยังไม่มีใครเกณฑ์ผู้ทดสอบมาทำเลยในรอบนี้

| TC | Req ID | Pri | ผล | รอบที่ | หลักฐาน | BUG | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| TC-074 | UX-02 | P0 | ยังไม่ได้รัน | — | — | — | ต้องใช้ผู้ทดสอบจริง |
| TC-075 | UX-05 | P1 | ยังไม่ได้รัน | — | — | — | ต้องใช้ผู้ทดสอบจริง |
| TC-076 | UX-07 · BR-3 | P0 | ยังไม่ได้รัน | — | — | — | ต้องใช้ผู้ทดสอบจริง ≥10 คน |
| TC-077 | UX-08 · BR-12 | P0 | ยังไม่ได้รัน | — | — | — | ต้องใช้ผู้ทดสอบจริง ≥10 คน |
| TC-078 | UX-09 | P0 | ยังไม่ได้รัน | — | — | — | ต้องวัดขนาดจริงบนอุปกรณ์จริง |
| TC-079 | UX-10 | P0 | ยังไม่ได้รัน | — | — | — | ต้องใช้ผู้ทดสอบจริง |
| TC-080 | UX-11 | P0 | ยังไม่ได้รัน | — | — | — | ต้องใช้ผู้ทดสอบจริง ≥5 คน |
| TC-081 | NFR-20 · UX-09 | P0 | ยังไม่ได้รัน | — | — | — | ต้องใช้ผู้ทดสอบจริง ≥5 คน สวมถุงมือเปียก |

### S7 — Error / Empty / Edge path (TC-082…097)

| TC | Req ID | Pri | ผล | รอบที่ | หลักฐาน | BUG | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| TC-082 | UX-13 | P0 | ยังไม่ได้รัน | — | — | — | ไม่พบเทสสแกน QR ไม่มีในระบบโดยตรง — ต้องทดสอบด้วยมือ |
| TC-083 | UX-13 | P1 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-084 | UX-13 · US-42 | P0 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-085 | UX-14 | P0 | ยังไม่ได้รัน | — | — | — | เป็นเรื่องการแสดงผลบนจอ — ต้องทดสอบด้วยมือ |
| TC-086 | UX-14 · US-35 | P1 | **PASS** | 1 | `tests/integration/admin.test.ts` > "A-05 เมล็ดพิเศษ" > "เหลือเปิดแม้ตัวเดียว → ลูกค้ายังเห็นกลุ่ม และเห็นเฉพาะตัวที่เปิด" | — | ครอบส่วน "ยังเหลือตัวเลือกอื่นอยู่" — สไตล์ disabled+label บนจอยังไม่ถูกเทส |
| TC-087 | UX-15 · US-26 | P1 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-088 | UX-16 | P1 | ยังไม่ได้รัน | — | — | — | ต้องทดสอบด้วยมือ |
| TC-089 | **UX-17** | P0 | ยังไม่ได้รัน | — | — | — | ต้องจำลองตัดเน็ตจริงบนเบราว์เซอร์ — ต้องทดสอบด้วยมือ |
| TC-090 | UX-17 | P0 | ยังไม่ได้รัน | — | — | — | กลไก Idempotency-Key ถูกเทสในหลายบริบทแล้ว (counter/table-orders/concurrency) แต่ไม่ใช่ในสถานการณ์ "ตัดเน็ตแล้วต่อกลับ" ตรงตามเคสนี้ — ต้องทดสอบด้วยมือ |
| TC-091 | UX-18 | P0 | ยังไม่ได้รัน | — | — | — | ตรรกะ idempotency มีเทสรองรับทั่วไป แต่ "กดรัว 5 ครั้งติดจากอุปกรณ์จริง + ปุ่มถูกปิดทันที" เป็น UI — ต้องทดสอบด้วยมือ |
| TC-092 | UX-14 · NFR-08 | P0 | **PASS** | 1 | `tests/integration/concurrency.test.ts` > "§5.2" > "สินค้าหมดตอนกดยืนยัน → reject รายการนั้นรายการเดียว บิลไม่ล้ม (HTTP 201)" | — | — |
| TC-093 | UX-14 | P0 | **PASS** | 1 | `tests/integration/concurrency.test.ts` > "§5.2" > "ทุกรายการหมด → 409 ALL_ITEMS_UNAVAILABLE และไม่สร้างออเดอร์เลย" | — | — |
| TC-094 | UX-19 · US-29 | P0 | ยังไม่ได้รัน | — | — | — | ไม่พบเทสสถานการณ์ "ลูกค้าเดินหนีไม่จ่ายเงิน" โดยตรง — ต้องทดสอบด้วยมือ |
| TC-095 | UX-17 · US-19 | P0 | **PASS** | 1 | `tests/integration/concurrency.test.ts` > "§5.3" > "สั่งเพิ่มหลังบิลปิด → 409 SESSION_CLOSED และตะกร้าไม่หาย" | — | — |
| TC-096 | US-20 · BR-5 | P0 | ยังไม่ได้รัน | — | — | — | ไม่พบเทสฝั่งลูกค้าพยายามยกเลิกเองโดยตรง (403 FORBIDDEN_FOR_GUEST) — ต้องทดสอบด้วยมือ/เทสเพิ่ม |
| TC-097 | US-42 | P0 | **PASS** | 1 | `tests/integration/admin.test.ts` > "A-06 จัดการโต๊ะ" > "ปลดระวางโต๊ะที่มีบิลเปิด → บล็อก พร้อมบอกว่าต้องปิดบิลก่อน" | — | — |

### S8 — Race condition (TC-098…103)

> ⚠️ **หมายเหตุระเบียบวิธี**: เอกสารต้นฉบับ (test-case-v1 §S8) กำหนดว่าเคสกลุ่มนี้ "ต้องใช้อุปกรณ์ ≥2 เครื่องกดพร้อมกันจริง" เทสอัตโนมัติที่มีอยู่ใช้การยิงคำขอพร้อมกันจริงในโค้ด (`Promise.all`) ไม่ใช่อุปกรณ์จริง 2 เครื่อง — พิสูจน์ race condition ที่ระดับเซิร์ฟเวอร์/ฐานข้อมูลได้จริง (แม่นยำกว่ามือมนุษย์กดพร้อมกันด้วยซ้ำ) แต่ **ไม่ครอบมุมมอง UI บนอุปกรณ์จริง** จึงยังทำเครื่องหมาย PASS โดยระบุข้อจำกัดนี้ไว้ทุกแถว

| TC | Req ID | Pri | ผล | รอบที่ | หลักฐาน | BUG | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| TC-098 | §5.1 · BR-2 · NFR-05 | P0 | ยังไม่ได้รัน | — | — | — | มีเทสใกล้เคียง (`concurrency.test.ts` §5.1 เพิ่ม*สินค้าเดียวกัน*พร้อมกัน) แต่ TC-098 ต้องการ*สินค้าคนละตัว*พร้อมกัน — ไม่พบเทสตรง จึงลงยังไม่ได้รัน |
| TC-099 | §5.2 · BR-4 | P0 | **PASS*** | 1 | `tests/integration/concurrency.test.ts` > "§5.2" > "สินค้าหมดตอนกดยืนยัน → reject รายการนั้นรายการเดียว บิลไม่ล้ม" | — | *ยิงพร้อมกันจริงในโค้ด ไม่ใช่อุปกรณ์จริง — ดูหมายเหตุระเบียบวิธีข้างบน |
| TC-100 | **§5.3** | **P0** | **PASS*** | 1 | `tests/integration/concurrency.test.ts` > "§5.3 — ปิดบิลชนกับการสั่งเพิ่ม" > "ยอดเปลี่ยนไปแล้ว → 409 BILL_CHANGED พร้อมยอดใหม่ ห้ามปิดเงียบ ๆ" | — | *เดียวกับข้างบน — เคสความเสี่ยงเงินจริงตามที่เอกสารเน้น |
| TC-101 | §5.4 | P0 | **PASS*** | 1 | `tests/integration/concurrency.test.ts` > "§5.4" > "3 เครื่องกดพร้อมกัน → สถานะสุดท้ายไม่ย้อนกลับ" | — | *เดิมอยู่กลุ่ม degraded (ต้องบัญชีเดียวหลายเครื่อง) — ปลดล็อกแล้วด้วยเทสจำลอง concurrency |
| TC-102 | §5.5 | P0 | **PASS*** | 1 | `tests/integration/counter.test.ts` > "US-29..." > "🔴 สองเครื่องกดรับเงินพร้อมกันจริง → payment แถวเดียว" | — | *เดิมอยู่กลุ่ม degraded — ปลดล็อกแล้ว ตรงตามชื่อเคสเป๊ะ |
| TC-103 | §5.6 · US-42 | P1 | ยังไม่ได้รัน | — | — | — | ไม่พบเทสแข่งกันระหว่างปลดระวางโต๊ะกับสร้างบิลใหม่ — ต้องทดสอบด้วยมือ/เทสเพิ่ม |

### S9 — DB Invariant (TC-104…113)

> เกือบทั้งหมดแม็ปตรง 1:1 กับ `tests/integration/invariants.test.ts` เพราะทั้งไฟล์เขียนขึ้นมาเพื่อพิสูจน์ INV ชุดนี้โดยเฉพาะ ยิง SQL ตรงเข้าฐานข้อมูลตามที่เอกสารต้นฉบับกำหนดไว้เป๊ะ

| TC | Req ID | ผล | รอบที่ | หลักฐาน | BUG | หมายเหตุ |
|---|---|---|---|---|---|---|
| TC-104 | INV-1 | **PASS** | 1 | `invariants.test.ts` > "INV-1..." > "เปิดรอบที่สองบนจุดบริการเดิมต้องถูกปฏิเสธ" + "ปิดรอบแรกแล้วเปิดรอบใหม่ได้" | — | — |
| TC-105 | INV-2 | **PASS** | 1 | `invariants.test.ts` > "INV-2..." > "เพิ่ม QR ใบที่สองให้จุดบริการเดิมต้องถูกปฏิเสธ" | — | — |
| TC-106 | INV-3 | **PASS** | 1 | `invariants.test.ts` > "INV-3..." > "เลื่อน awaiting_payment → queued ตอนบิลยังไม่จ่าย ต้องถูกปฏิเสธ" | — | — |
| TC-107 | INV-4 | **PASS** | 1 | `invariants.test.ts` > "INV-4..." > "สร้างออเดอร์ table + awaiting_payment ต้องถูกปฏิเสธ" | — | — |
| TC-108 | INV-5 | **PASS** | 1 | `invariants.test.ts` > "INV-5..." > "ไม่ระบุผู้สร้างต้องถูกปฏิเสธ" | — | — |
| TC-109 | INV-6 · BR-5 | **PASS** | 1 | `invariants.test.ts` > "INV-6..." > "ตั้งสถานะ cancelled โดยไม่ใส่ cancel_reason ต้องถูกปฏิเสธ" + "เหตุผลที่เป็นช่องว่างล้วนก็ต้องถูกปฏิเสธ" | — | — |
| TC-110 | INV-7 | **PASS** | 1 | `invariants.test.ts` > "INV-7..." > "ต้องถูกปฏิเสธ" | — | — |
| TC-111 | INV-8 | **PASS** | 1 | `invariants.test.ts` > "INV-8..." > "ปิด visit_session ที่บิลยัง open ต้องถูกปฏิเสธ" | — | — |
| TC-112 | INV-9 | ยังไม่ได้รัน | — | — | — | **ไม่มีในชุด `pnpm test` เลย** — มี job แยกต่างหาก `pnpm check:invariants` ตาม CLAUDE.md ของ repo แต่งานนี้ไม่ได้รับอนุญาตให้รันคำสั่งเพิ่มเติมนอกเหนือผลที่ได้รับมา จึงยืนยันไม่ได้ว่าผ่านหรือไม่ |
| TC-113 | INV-10 · BR-2 | **PASS** | 1 | `invariants.test.ts` > "INV-10..." > "เพิ่มบรรทัดเดิมซ้ำได้ผลเป็นการรวมจำนวน ไม่ใช่สองบรรทัด" | — | — |

### S10 — ผลของคำตัดสินรอบ 2026-08-16 (TC-114…116)

| TC | Req ID | Pri | ผล | รอบที่ | หลักฐาน | BUG | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| TC-114 | US-30 · US-14 · BR-12 | P0 | **BLOCKED** | 1 | อ่านโค้ดจริง: ไม่มีค่าสถานะ "รอเสิร์ฟ" ใน `order_status` enum เลย (`migrations/006_order_money.sql`) และไม่มี label นี้ที่ไหนในโค้ด | — | คำตัดสินของ Touch 2026-08-16 ยังไม่ถูก implement — ยังบล็อกเหมือนเดิม แม้เลขคิว (อีกครึ่งของ G-1) จะปลดล็อกแล้ว |
| TC-115 | US-30 · US-14 | P0 | **BLOCKED** | 1 | เดียวกับ TC-114 | — | เดียวกับ TC-114 |
| TC-116 | **US-50** | P0 | ยังไม่ได้รัน | — | — | — | พบว่ามีกลุ่มตัวเลือก "ชนิดนม" อยู่ในโค้ดแล้ว (`option-prices.test.ts` ใช้กลุ่มนี้เป็นตัวอย่าง fixed group) แต่ไม่พบเทสยืนยันเกณฑ์เฉพาะของ TC-116 (แสดงเฉพาะสินค้าที่มีนม, ไม่มีส่วนต่างราคา, ขึ้นตั๋วพนักงาน) — ต้องทดสอบด้วยมือ |

---

## 4. ผล UAT

ตาม [[../01-test-plan/uat-scenario-v1|UAT Scenario v1]] — แยกจากผล test case เพราะวัดคนละอย่าง

| UAT | ผู้ทดสอบ (กี่คน) | สำเร็จ / ทั้งหมด | เข้าใจถูก / ทั้งหมด | ผ่านเกณฑ์ | คำพูดดิบที่ควรจำ |
|---|---|---|---|---|---|
| — | *(ยังไม่มี — รอบนี้ทำเฉพาะเทสอัตโนมัติ ยังไม่ได้เกณฑ์ผู้ทดสอบจริงมา UAT)* | | | | |

> **จดคำพูดดิบของผู้ทดสอบไว้เสมอ** โดยเฉพาะประโยคที่แสดงความเข้าใจผิดเรื่องการจ่ายเงิน — เป็นวัตถุดิบที่ [[../../04-retrospectives/index|04-retrospectives]] ใช้ได้จริง และหาย้อนหลังไม่ได้ถ้าไม่จดตอนนั้น

---

## 5. เคสที่ยังรันไม่ได้ ณ วันนี้

> ~~ยกมาจาก [[../01-test-plan/test-plan-v1|Test Plan v1]] §5 — **18 เคส** ที่รู้ล่วงหน้าแล้วว่าจะรันไม่ได้~~
> **อัปเดต 2026-08-28 หลังตรวจโค้ดจริงทีละกลุ่ม**: ของเดิม 18 เคสไม่ได้ "ยังบล็อกทั้งหมด" อีกต่อไป — 2 ใน 4 กลุ่มปลดล็อกไปมากแล้ว ตารางด้านล่างคือสถานะจริงวันนี้ (คอลัมน์ "สถานะเดิม" คงไว้ตามที่ [[../01-test-plan/test-plan-v1|Test Plan v1]] §5 เขียนไว้ ไม่ได้ลบทิ้ง)

| กลุ่ม | เคส | สถานะเดิม (2026-08-16) | สถานะจริงวันนี้ (2026-08-28) | ปลดล็อกโดย |
|---|---|---|---|---|
| **G-1a** เลขคิว (ส่วนหนึ่งของ G-1 เดิม) | TC-052, TC-053, TC-054 | บล็อก — ไม่มีคอลัมน์เลขคิว | 🟢 **ปลดล็อกแล้ว** — คอลัมน์ `queue_no` มีแล้ว (migration 015) มีเทสอัตโนมัติผ่านครบ 3 เคส | เดิมมอบ COULSON — D-2 (ทำเสร็จแล้ว) |
| **G-1a ต่อ** | TC-055, TC-055b | บล็อก | 🟡 **ยังไม่ปลดล็อกเต็มที่** — ฟีเจอร์มีแล้วแต่ยังไม่มีเทสอัตโนมัติแบบยิงพร้อมกันจริง (TC-055) / ยังต้องทดสอบด้วยมือกับคนจริง (TC-055b) | — |
| **G-1a ต่อ** | TC-055a | บล็อก (ไม่มีเครื่องพิมพ์) | 🔴 **ยังบล็อกเหมือนเดิม** — ไม่มีเครื่องพิมพ์จริงในสภาพแวดล้อมทดสอบ | ต้องมีเครื่องพิมพ์จริง |
| **G-1b** สถานะ "รอเสิร์ฟ" (อีกส่วนของ G-1 เดิม) | TC-114, TC-115 (+ พบเพิ่มว่ากระทบ TC-016a, TC-017, TC-018, TC-032 ด้วย ซึ่งไม่เคยอยู่ในสารบัญ 18 เคสเดิม) | บล็อก — รอ schema change | 🔴 **ยังบล็อกเหมือนเดิม — และขยายวงกว้างกว่าที่เอกสารเดิมระบุไว้** ไม่มีค่า/label "รอเสิร์ฟ" อยู่ในโค้ดแม้แต่จุดเดียว (ยืนยันด้วย grep เต็ม repo) คำตัดสินของ Touch 2026-08-16 ยังไม่ถูกทำ | COULSON — D-2 (ยังไม่เริ่ม) |
| **G-2** ป้าย QR | TC-049…051 | บล็อก — ยังไม่มีแบบป้าย | 🟢 **ปลดล็อกเกือบหมด** — TC-049, TC-050 มีเทสผ่านแล้ว เหลือ TC-051 บล็อกเฉพาะส่วนวัด/สแกนจริงเพราะไม่มีเครื่องพิมพ์ | SHURI + COULSON — D-1/D-2 (ทำเสร็จแล้วเป็นส่วนใหญ่) |
| **G-3** auth NFR-06/10/11 | TC-062, TC-063, TC-066, TC-067 | บล็อก | 🟡 **ปลดล็อกบางส่วน** — TC-067 (NFR-11 PIN unlock) มีเทสผ่านแล้ว · TC-062/TC-066 มี logic รองรับในโค้ดแล้วแต่ยังไม่มีเทสอัตโนมัติยืนยัน (ไม่ใช่บล็อกเพราะฟีเจอร์ขาดอีกต่อไป แค่ยังไม่ได้ทดสอบ) · TC-063 ยังต้องทดสอบ realtime จริง | COULSON — D-3 (ทำไปมากแล้ว) |
| **degraded** ต้องใช้บัญชีเดียวหลายเครื่อง | TC-059, TC-101, TC-102 | degraded | 🟢 **TC-101, TC-102 ปลดล็อกแล้ว** ด้วยเทส concurrency ที่จำลองคำขอพร้อมกันจริงแทนอุปกรณ์จริงได้ (ดูหมายเหตุระเบียบวิธี S8) · 🟡 **TC-059 ยังต้องทดสอบด้วยมือ** เป็นเรื่อง timing ของจอจริงหลายเครื่อง | COULSON — D-3 |
| **ใหม่** ฟีเจอร์ที่เอกสารเขียนไว้แต่โค้ดยังไม่สร้างเลย (ไม่เคยอยู่ในสารบัญ 18 เคสเดิม) | TC-072c (job ล้างชื่อเล่นอายุ 90 วัน) | — | 🔴 **บล็อกใหม่ที่เพิ่งพบ** — ค้นทั้ง repo ไม่พบ job นี้เลย | ต้องมีคนสร้างฟีเจอร์นี้ก่อน |

🔴 **ทั้งหมดนี้ยังอยู่ใน exit criteria ของ Phase 0** — ตราบใดที่ TC-114/115 (และเคสที่พบเพิ่มว่าพ่วงมาด้วย) ยังบล็อกอยู่ **ห้ามรายงานว่า Phase 0 ผ่านการทดสอบ** ไม่ว่าเคสที่เหลือจะ PASS ครบแค่ไหน — และตอนนี้พบว่าผลกระทบของสถานะ "รอเสิร์ฟ" ที่ยังไม่ถูกทำ **กว้างกว่าที่เอกสารเดิมประเมินไว้**

---

## 6. เคสอัตโนมัติที่มีจริงแต่ไม่ตรงกับ TC ใดเลยในเอกสาร

> รายการนี้คือสิ่งที่โค้ด/เทสทำแต่แผนทดสอบ (test-case-v1, ปิดเนื้อหา 2026-08-16) ไม่เคยคิดถึง — ส่วนใหญ่เป็นฟีเจอร์ที่ถูกสร้างหลังวันที่เอกสารปิดเนื้อหา ควรพิจารณาเพิ่มเป็น TC ใหม่ (ดู [[../01-test-plan/test-case-v1|Test Case Specification v1]] §2 กติกาเพิ่มเคส — ใช้เลขถัดจาก TC-116)

| ไฟล์เทส | สิ่งที่ทดสอบ | ทำไมไม่มีใน test-case-v1 |
|---|---|---|
| `tests/integration/promptpay.test.ts` (7 เคส) | สร้าง QR พร้อมเพย์สำหรับรับเงิน (payload ตรง spec ธนาคาร, CRC, byte-level) | ฟีเจอร์ PromptPay QR ทั้งฟีเจอร์ไม่ปรากฏใน backlog/TC ที่ใช้เขียนเอกสารนี้เลย — ดูเหมือนถูกเพิ่มเข้ามาทีหลัง |
| `tests/integration/menu-order.test.ts` (8 เคส) | เรียงลำดับเมนู/หมวดหมู่เองได้ (custom drag order) | ไม่มี US/TC ใดพูดถึงการเรียงลำดับเมนูเองเลย |
| `tests/integration/option-prices.test.ts` (9 เคส) | ตั้งราคาเพิ่มต่อตัวเลือก (option add-on pricing) รวมกลุ่ม fixed ที่เดิมไม่ให้แก้ | TC เดิมพูดถึงตัวเลือกเป็นของฟรีเสมอ ไม่มีเคสเรื่องราคาต่อตัวเลือก |
| `tests/integration/category-option-defaults.test.ts` (5 เคส) | ตั้งค่า default ของกลุ่มตัวเลือกแยกรายหมวดหมู่ | ไม่มี US/TC ที่พูดถึงการตั้งค่า default ระดับหมวดหมู่ |
| `tests/integration/product-icon.test.ts` (4 เคส) | แมปไอคอนแทนรูปสินค้าตามชื่อเมนู | ไม่มีใน TC เดิม — เป็น fallback UI ที่ถูกเพิ่มทีหลัง |
| `tests/integration/next-image-config.test.ts` (3 เคส) | ตั้งค่า Next.js image config ให้รับ query string cache-busting | เป็นเทสระดับ infra/config ไม่ใช่ requirement ระดับ TC |
| `tests/integration/signage-contrast.test.ts` (4 เคส) | ฟังก์ชันคำนวณ contrast ratio ของสีป้าย | ใกล้เคียง TC-073 (a11y) แต่เป็นเครื่องมือคำนวณเฉพาะป้าย ไม่ใช่เกณฑ์ที่ TC-073 ระบุไว้ (ทั้ง 7 หน้าจอลูกค้า) |
| `tests/integration/qr-signage-raster.test.ts` (ส่วนเกิน TC-049/050) | พิมพ์ QR หลายใบต่อแผ่น A4 (4 ใบ/แผ่น, ใบที่ 5 ขึ้นแผ่นใหม่), พื้นหลัง JPEG/PNG, ขอบขาวล้อมรอบ | ไม่มี TC ใดพูดถึงการจัดหน้า A4 หลายใบต่อแผ่นเลย — TC-049/050/051 พูดถึงป้ายใบเดียว |
| `tests/unit/design-tokens.test.ts` (1 เคส) | ตรวจว่าทุก `var(--x)` ที่ถูกอ้างถึงมีการประกาศไว้จริง | เป็นเทสระดับ build-hygiene ไม่ใช่ requirement |
| `tests/integration/multi-device.test.ts` > "readCustomerContext" | ไม่มี cookie → ไม่มีบริบท ไม่ throw | เป็น unit-level edge case ของฟังก์ชันภายใน ไม่ผูกกับ TC ใดโดยตรง |
| `tests/integration/tenant-isolation.test.ts` > "ไม่มี qr_code.code ซ้ำกันข้ามร้าน (unique ทั้งตาราง)" | QR code unique ทั้งระบบข้ามร้าน (ลึกกว่า INV-2 ที่พูดถึงแค่ต่อจุดบริการ) | เป็นเกณฑ์ระดับระบบที่ไม่มี TC ไหนพูดถึงตรง ๆ |

---

## 7. การเพิ่ม/แก้เคส

*(คงตามกติกาเดิมของ [[../01-test-plan/test-case-v1|Test Case Specification v1]] §2 — เอกสารนี้ไม่มีสิทธิ์แก้ตัวเคส มีแต่บันทึกผล)*
