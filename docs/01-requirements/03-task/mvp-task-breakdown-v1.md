# MVP Task Breakdown v1 — Phase 0

> แตกจาก [[../02-plan/release-roadmap-v1|02-plan/release-roadmap-v1]] เฉพาะ Phase 0 (MVP) ให้เป็นชิ้นงานที่ลงมือทำได้จริง อ้างอิง user story เต็มได้ที่ [[../01-spec/product-backlog-v1|01-spec/product-backlog-v1]]
> นี่คือ **task ระดับ requirement/scope** สำหรับส่งต่อให้ COULSON แตกเป็นงานออกแบบ/สถาปัตยกรรมจริงใน `02-design` ไม่ใช่ spec เชิงเทคนิค (ไม่มี DB schema/component design ในเอกสารนี้)
> จัดทำโดย: XAVIER · วันที่ 2026-08-02
> **อัปเดต 2026-08-02:** Touch ยืนยัน 2 กฎธุรกิจที่กระทบ data model ของ Phase 0 — (1) QR ต่อโต๊ะเป็นแบบ static ไม่มี flow regenerate และ (2) สถานะโต๊ะ (เปิด/ว่าง) ต้อง derive จากวงจรชีวิตออเดอร์ ไม่ใช่ manual toggle ดูรายละเอียดเต็มใน Business Rule ข้อ 10-11 ของ [[../01-spec/product-backlog-v1|01-spec/product-backlog-v1]] — งานในกลุ่ม A และ E ด้านล่างอัปเดตให้สอดคล้องแล้ว

กลับไปที่ [[index|03-task]]

สถานะ: ยังไม่เริ่ม (Not started) — ทั้งหมดรอส่งต่อ COULSON เริ่ม `02-design`

---

## กลุ่มงาน A — โต๊ะ และ QR Code (รองรับ US-01, US-24)

- [ ] กำหนดจำนวนโต๊ะเริ่มต้นของร้านและรูปแบบเลขโต๊ะ (เช่น T01–T20)
- [ ] **(ยืนยันแล้ว)** QR ผูกกับ 1 โต๊ะแบบ static คงที่ตลอดไป ไม่มี flow regenerate/หมุนเวียน QR ต่อออเดอร์หรือต่อรอบลูกค้า (Business Rule ข้อ 10) — ส่งต่อ COULSON ออกแบบวิธี encode ข้อมูลโต๊ะใน QR โดยไม่ต้องเผื่อระบบสร้าง QR ใหม่อัตโนมัติ ลดสโคปที่ต้องออกแบบลงได้
- [ ] ผลิต/พิมพ์ QR code ต้นแบบอย่างน้อย 1 ใบเพื่อทดสอบ flow จริงก่อนสั่งพิมพ์เต็มจำนวนโต๊ะ (พิมพ์ครั้งเดียวใช้ได้ยาว เพราะเป็น static)
- [ ] กำหนดข้อความ error ที่ลูกค้าเห็นเมื่อ QR ใช้ไม่ได้ (หมดอายุ/โต๊ะไม่พบ)
- [ ] **(ใหม่)** ส่งต่อ COULSON: table entity ต้องมี status field ("ว่าง" / "ไม่ว่าง-มีลูกค้า") ที่อัปเดตอัตโนมัติจากเหตุการณ์ออเดอร์ (เปิด = ออเดอร์แรกของรอบเข้ามา, ปิด = แคชเชียร์ checkout เสร็จ) ไม่ใช่ field ที่พนักงานตั้งค่าด้วยมือ (Business Rule ข้อ 11) — กระทบ data model ของโต๊ะโดยตรง ต้องออกแบบ state ก่อน finalize schema

## กลุ่มงาน B — หน้าจอลูกค้า: เมนู + ตะกร้า + ส่งออเดอร์ (รองรับ US-02, US-03, US-04, US-06, US-07)

- [ ] ยืนยันรายการหมวดหมู่เมนูเริ่มต้นกับ Touch (เช่น กาแฟ / ชา / ของหวาน / ของทานเล่น) ก่อนส่งต่อออกแบบ
- [ ] เตรียมรายชื่อสินค้าตั้งต้น (ชื่อ ราคา รูป คำอธิบายสั้น) อย่างน้อยพอสำหรับเปิดร้านวันแรก — Touch เป็นผู้ให้ข้อมูลสินค้าจริง
- [ ] ส่งต่อ COULSON ออกแบบหน้าจอ: ดูเมนู → เพิ่มตะกร้า → แก้ไขจำนวน/ลบ → ยืนยันส่งออเดอร์ → ดูสรุปยอดรวมของโต๊ะ (multi-round)
- [ ] ยืนยัน flow ว่าออเดอร์แต่ละรอบส่งแยกกัน แต่ต้องรวมยอดในบิลเดียวกันของโต๊ะ (ตาม Business Rule ข้อ 1, 7)
- [ ] **(ใหม่)** ยืนยันกับ COULSON ว่าการยืนยันส่งออเดอร์ (US-04) ต้อง trigger การเปลี่ยนสถานะโต๊ะเป็น "ไม่ว่าง" โดยอัตโนมัติเฉพาะตอนเป็นออเดอร์แรกของรอบ (โต๊ะว่างอยู่ก่อนหน้า) — รอบถัดไปของโต๊ะเดียวกันไม่ต้อง trigger ซ้ำ (Business Rule ข้อ 11)

## กลุ่มงาน C — ติดตามสถานะออเดอร์ฝั่งลูกค้า (รองรับ US-05)

- [ ] ยืนยันกับ COULSON ว่าจะใช้กลไก real-time แบบไหน (polling หรือ push) — เป็นการตัดสินใจเชิงเทคนิค ไม่ใช่ของ XAVIER แต่ flag ไว้เป็นข้อกำหนดว่า "ต้องอัปเดตโดยลูกค้าไม่ต้องกดรีเฟรชเอง"
- [ ] กำหนดชื่อ/ลำดับสถานะที่ลูกค้าเห็นให้ตรงกับที่ครัวใช้ (รอดำเนินการ / กำลังทำ / เสร็จแล้ว)

## กลุ่มงาน D — หน้าจอครัว/บาร์ (รองรับ US-13, US-14, US-15, US-16)

- [ ] ส่งต่อ COULSON ออกแบบหน้าจอออเดอร์ใหม่แบบเรียลไทม์ + ปุ่มเปลี่ยนสถานะ (รอดำเนินการ/กำลังทำ/เสร็จแล้ว)
- [ ] กำหนดรูปแบบการแจ้งเตือน (เสียง/popup) ที่เหมาะกับหน้าร้านจริง (เสียงต้องได้ยินในที่มีเสียงรบกวน)
- [ ] ส่งต่อออกแบบหน้าจอ mark สินค้า "หมดชั่วคราว" ให้ครัวกดได้เองระหว่างงานยุ่ง (ต้องเร็ว ไม่กี่คลิก)
- [ ] ยืนยัน edge case: ถ้าลูกค้ายืนยันออเดอร์ในจังหวะที่สินค้าเพิ่งถูก mark หมด ระบบต้อง reject เฉพาะรายการนั้น ไม่ reject ทั้งบิล (ตาม Business Rule ข้อ 4)

## กลุ่มงาน E — หน้าจอแคชเชียร์ (รองรับ US-18, US-19, US-20)

- [ ] ส่งต่อ COULSON ออกแบบหน้าจอเลือกโต๊ะ → เห็นสรุปยอดรวมทุกออเดอร์ของโต๊ะนั้น (ควรแยกให้เห็นชัดว่าโต๊ะไหน "ไม่ว่าง" อยู่ตอนนี้ เพราะสถานะนี้ derive จากออเดอร์ ไม่ใช่ค่าที่พนักงานตั้งเอง)
- [ ] **(ยืนยันแล้ว)** ปุ่ม "ปิดบิล" คือจุดเดียวที่ทำให้สถานะโต๊ะเปลี่ยนกลับเป็น "ว่าง" — ไม่ต้องออกแบบปุ่ม/ฟังก์ชัน "เปิด/ปิดโต๊ะ" แยกต่างหากให้พนักงาน (Business Rule ข้อ 11) ส่งต่อ COULSON ออกแบบเฉพาะปุ่มปิดบิลที่เคลียร์ session และเปลี่ยนสถานะโต๊ะในจังหวะเดียวกัน
- [ ] ส่งต่อออกแบบหน้าจอยกเลิกออเดอร์/รายการ ที่บังคับให้กรอกเหตุผลก่อนยืนยันเสมอ (ตาม Business Rule ข้อ 5)

## กลุ่มงาน F — หน้าจอแอดมิน: จัดการเมนู (รองรับ US-23)

- [ ] ส่งต่อ COULSON ออกแบบหน้าจอเพิ่ม/แก้ไข/ลบสินค้า ราคา รูป หมวดหมู่
- [ ] ยืนยันว่าการแก้ไขเมนูมีผลกับฝั่งลูกค้าทันทีโดยไม่ต้อง deploy ใหม่ (ข้อกำหนด ไม่ใช่วิธีทำ)

## งานข้ามกลุ่ม (Cross-cutting)

- [ ] ยืนยันกับ Touch เรื่อง Business Rule ข้อ "จ่ายก่อนหรือหลัง" และ "แชร์ตะกร้าต่อโต๊ะ" ก่อนที่ COULSON จะ finalize data model ของออเดอร์/บิล (ดู NEED-INPUT ใน 01-spec ข้อ 5)
- [ ] เตรียม checklist ทดสอบ end-to-end ของ Phase 0 ให้ OKOYE ใช้ก่อนเปิดร้านจริง: สแกน QR → สั่ง → ครัวเห็น/อัปเดตสถานะ → ลูกค้าเห็นสถานะ → สั่งรอบสอง → แคชเชียร์ปิดบิล → โต๊ะว่างสำหรับลูกค้าคนถัดไป
- [ ] เก็บ log การตัดสินใจสำคัญ (เช่น default "จ่ายที่เคาน์เตอร์") ไว้ใน [[../../05-log/index|05-log]] เมื่อ Touch ยืนยันแล้ว

---

## สถานะ Handoff

Phase 0 ทั้งหมดข้างต้น **พร้อมส่งต่อ COULSON เริ่ม `02-design`** ได้ทันที ยกเว้นงานในหัวข้อ "งานข้ามกลุ่ม" ข้อแรก (payment timing + shared cart ต่อโต๊ะ) ที่ควรได้คำตอบจาก Touch ก่อน COULSON จะ finalize data model ของออเดอร์/บิล — ไม่บล็อกการเริ่มงานออกแบบหน้าจอ (UI) แต่บล็อกการ finalize schema

การยืนยันเรื่อง QR แบบ static (Business Rule ข้อ 10) และสถานะโต๊ะที่ derive จากออเดอร์ (Business Rule ข้อ 11) **ไม่ใช่ประเด็นเปิดอีกต่อไป และไม่ใช่ blocker ใหม่** — ตรงกันข้าม ทั้งสองข้อช่วยลดสโคปงานออกแบบของ COULSON ลง (ไม่ต้องออกแบบระบบ regenerate QR, ไม่ต้องออกแบบปุ่มเปิด/ปิดโต๊ะแยกต่างหาก) แต่มีผลโดยตรงต่อ data model ของ table entity (ต้องมี status field ที่ derive จากเหตุการณ์ออเดอร์) จึงถูกเพิ่มเป็น task ใหม่ในกลุ่มงาน A, B, E ด้านบน
